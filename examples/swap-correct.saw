// Load the bitcode files generated by Clang
swapmod <- llvm_load_module "swap-correct.bc";

// Extract a formal model of the `swap_correct` property
harness <- llvm_extract swapmod "swap_correct" llvm_pure;

// Use the ABC prover to show that it always returns non-zero.
prove_print abc {{ \x y -> harness x y != 0 }};
